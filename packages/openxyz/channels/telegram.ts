// Popular channel — re-exported from `@openxyz/channel-telegram` so templates
// can `import { TelegramChannel } from "openxyz/channels/telegram"` without
// an extra npm install. Less-popular channels ship as standalone packages
// (`@openxyz/channel-<slug>`) that users add explicitly.
export * from "@openxyz/channel-telegram";
